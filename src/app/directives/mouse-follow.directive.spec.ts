import { MouseFollowDirective } from './mouse-follow.directive';

describe('MouseFollowDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseFollowDirective();
    expect(directive).toBeTruthy();
  });
});
